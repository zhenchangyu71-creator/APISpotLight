# OpenAI-compatible multimodal endpoint used for screenshot analysis.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
VISION_MODEL=gpt-4o-mini

# Torna project-doc pull (header token; never pass via tool args).
TORNA_TOKEN=
# Optional overrides
TORNA_TIMEOUT_SECONDS=30
TORNA_DOC_CONCURRENCY=6
